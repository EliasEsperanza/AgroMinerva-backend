server:
  port: 8080

spring:
  application:
    name: servicio-comercial
  datasource:
    url: jdbc:postgresql://postgres_comercial:5432/comercial_db
    username: postgres
    password: TE22003
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
  sql:
    init:
      platform: postgresql

logging:
  level:
    com.agrominerva.comercial: DEBUG
    org.hibernate.SQL: DEBUG